In order for this package to work, you need to install canvas:

On MacOS, run `brew install pkg-config cairo pango libpng jpeg giflib librsvg pixman` before installing the package.

TODO:

- [ ] Add TODO List
